import { useEffect } from "react";
import { GiftedChat } from "react-native-gifted-chat";
import io from "socket.io-client";

const useConvoSocketConnection = (
  conversationID: string,
  setMessages: any
) => {};

export default useConvoSocketConnection;
